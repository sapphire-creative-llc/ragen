<div class="flex items-center justify-center text-center h100p w100p">
  <div class="header-color px1">
    <h1 class="m0 text-normal text-shadow">{{ section.settings.title }}</h1>
    <p class="m0 text-normal text-shadow">{{ section.settings.description}}</p>
    <a href="{{ section.settings.button_url }}" class="button white no-underline mt1 bg-black">
      {{ section.settings.button_text }}
    </a>
  </div>
</div>
